export default function ResponsiveHint() {
  return (
    <section className="responsive-hint">
      <div>
        <p className="eyebrow">Responsive Layout</p>
        <h2>Built for wide, half-screen, and mobile use.</h2>
      </div>
      <p>
        Panels now soften into cleaner rows on smaller screens so the workspace does not feel crushed.
      </p>
    </section>
  );
}
